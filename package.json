{
  "name": "publisher-exit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": { "start": "node server.mjs", "test": "node --test server.test.mjs", "check": "node --check server.mjs && node --check app.js" }
}
